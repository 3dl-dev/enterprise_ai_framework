# Chat surface configuration.
#
# One control plane: the model catalogue and the upstream come from here, pushed by the
# bundle — the surface is not administered through its own console.
#
# The surface holds a virtual key, never a provider key. Everything it sends goes to the
# gateway, so chat traffic lands in the same ledger and the same audit trail as the
# coding agents.

# Config schema version. Must equal Constants.CONFIG_VERSION in the pinned LibreChat
# image (packages/data-provider/src/config.ts) — 1.3.13 for v0.8.7, 1.2.8 for v0.8.0.
# Asserted against the RUNNING container rather than a hardcoded string by
# tests/test_chat_surface_version.py::test_config_schema_version_matches_the_running_image.
#
# A mismatch is advisory, not fatal: packages/api/src/app/checks.ts#checkConfig only
# logs "Outdated Config version". That is precisely why it is worth a test — nothing
# else in the stack notices, and the drift is only visible in a log line nobody reads.
version: 1.3.13
cache: true

interface:
  # No local account management — Keycloak owns identity.
  privacyPolicy:
    externalUrl: ""
  termsOfService:
    externalUrl: ""
  # INERT — and left here deliberately, with a test holding it that way.
  # `artifacts` is not a key of librechat-data-provider's `interfaceSchema` in v0.8.0 or
  # v0.8.7, so zod strips it, and packages/data-schemas/src/app/interface.ts never reads
  # it; it is correspondingly absent from what the surface serves at /api/config. What
  # actually turns on the Claude-like rendering of code and HTML is `preset.artifacts` on
  # each model spec below, which is where the real setting lives and where
  # tests/test_chat_surface_version.py asserts it.
  # Kept so this note survives: the next person to find artifacts broken must not "fix"
  # it here. test_the_interface_artifacts_key_is_inert fails if a release starts
  # honouring the key, which is when this comment needs rewriting.
  artifacts: true
  # Surfaces the memories panel in Settings so a user can see/edit what was stored.
  # Purely a UI toggle — does not gate whether memory is extracted (see `memory` below).
  memories: true
  # THE OPPOSITE TRAP FROM `artifacts` ABOVE (enterpriseaiframework-282): these two keys
  # ARE real, honoured zod fields (interfaceSchema in data-provider) — and BOTH default to
  # `true` only in the schema's own `.default({...})`, which zod applies solely when the
  # whole `interface:` key is missing from the parsed YAML. The moment this file sets
  # `interface:` at all (it has to, for privacyPolicy/termsOfService/artifacts/memories
  # above), every key we do NOT also list here comes back `undefined` from zod — which
  # `/api/config` then serves as `false`, not as "defaulted to true". Measured against the
  # running v0.8.7 surface before this line existed: `interface.modelSelect: false` and
  # `interface.parameters: false` in the live `/api/config` response, with this exact
  # librechat.yaml otherwise unchanged. `modelSelect: false` hides the model dropdown —
  # the picker can offer the full gateway catalogue all it wants
  # (endpoints.custom[].models.fetch below) and nobody can reach it. `parameters: false`
  # hides the Parameters panel, which is the ONLY place `reasoning_effort` (a real
  # tConversationSchema field, sent straight through to the model on `custom` endpoints —
  # see @librechat/api's getOpenAILLMConfig) is exposed to a user. Both are therefore
  # load-bearing for this item's done-condition and are set explicitly rather than relied
  # on as defaults that silently were not being applied.
  modelSelect: true
  parameters: true

# LibreChat's native memory (api/server/routes/memories.js + the `memory` Mongo
# collection in data-schemas) — CONFIGURED, not built. A durable per-user preference
# stated in one conversation is available in every later conversation because it is
# read from Mongo on every turn (api/server/controllers/agents/client.js #useMemory),
# not from anything held in the chat process — a deployment restart does not lose it.
#
# `agent.provider` must equal an `endpoints.custom[].name` below EXACTLY
# (server/services/Endpoints/index.js#getProviderConfig falls through to
# getCustomEndpointConfig by exact string match when the provider isn't one of the
# built-in EModelEndpoint/Providers names) — so the memory-extraction agent is routed
# through our own gateway on the chat surface's virtual key, same as any other model
# call: same ledger, same audit trail, no second credential.
#
# Model choice is the operator's (CHAT_MODEL_PRIMARY). Memory extraction is a
# tool call (`set_memory`/`delete_memory`) on every turn, and glm-5.2 is the model
# this bundle has actually measured invoking tools reliably (wave 1). Getting this
# wrong is silent (the model just answers instead of calling the tool) rather than a
# visible error, so it is not a place to guess.
#
# Left on the library's default instructions/behavior (see librechat's
# getDefaultInstructions): memory is written only when the user explicitly asks to be
# remembered ("remember that...", "don't forget..."), not opportunistically mined from
# every message — matching the item's own scenario ("a user tells the chat a durable
# preference").
#
# `agent.enabled: true` is REQUIRED from v0.8.6 onwards and was not required at v0.8.0.
# packages/data-schemas/src/app/memory.ts#isMemoryAgentEnabled now returns false unless
# `config.agent.enabled === true`, so the same config that extracted memories on v0.8.0
# extracts nothing on v0.8.7 — the memories panel still renders, the routes still answer,
# and every conversation simply stops learning anything. The only signal is one startup
# line: "[memory] Agent config detected without explicit `enabled: true`". Found by
# reading the v0.8.7 container's own boot log during enterpriseaiframework-f50, not by
# reading the changelog, which does not mention it.
#
# Safe on v0.8.0 as well: its memorySchema agent union is a plain (non-strict) z.object,
# so an unknown `enabled` key is stripped rather than rejected, and v0.8.0 extracted
# unconditionally anyway.
memory:
  personalize: true
  messageWindowSize: 5
  agent:
    enabled: true
    provider: "Enterprise AI"
    model: "__CHAT_MODEL_PRIMARY__"

# Defaults. Without these the surface opens with nothing selected and the user has to
# know which of eleven models to pick before they can type anything — which reads as
# "unconfigured", not "flexible".
modelSpecs:
  enforce: false        # the full catalogue stays reachable
  prioritize: true      # but open on a sensible default
  list:
    - name: glm-5-2
      label: "GLM 5.2"
      default: true
      description: "Open weights. $0.93/$3.00 per Mtok — about a third of Sonnet 5, a ninth of Opus."
      # webSearch (enterpriseaiframework-222) — A TOP-LEVEL MODELSPEC FIELD, SIBLING OF
      # `preset:` BELOW, NOT NESTED INSIDE IT. Getting this placement wrong is silent
      # twice over, and both wrong shapes were tried and measured against the live
      # cluster before this one:
      #
      #   1. `preset.webSearch: true` (camelCase, nested under preset) — parses fine,
      #      reaches nothing. The client's `applyModelSpecEphemeralAgent`
      #      (bundled as `dk()` in this pinned v0.8.7 image, confirmed by reading the
      #      RUNNING container's own `client/dist/assets/hooks.*.js`) computes a new
      #      conversation's `ephemeralAgent.web_search` from `modelSpec.webSearch`,
      #      i.e. the TOP-LEVEL property of the modelSpec object — never from
      #      `modelSpec.preset.webSearch`. Nesting it under `preset:` (where
      #      `executeCode` already lived, copied without checking) means the client
      #      never sees it, tool or no tool.
      #   2. `preset.web_search: true` (snake_case, nested under preset) — DOES survive
      #      the server's config-parsing schema and DOES appear in the authenticated
      #      `/api/config` response (confirmed by reading it back), but at
      #      `modelSpecs[].preset.web_search`, which is still the wrong path for the
      #      same client function above — it reads `.webSearch` (camelCase) off the
      #      modelSpec object directly, not off `.preset` at all, so this shape also
      #      reached the browser but still never reached `ephemeralAgent`.
      #
      # Without a real `ephemeralAgent.web_search` in the request body, a first turn
      # gets no web_search tool attached at all — even though the promptPrefix below
      # unconditionally instructs the model to "Search the web when...". LibreChat never
      # executes text shaped like a tool call, so a model told to use a tool it was
      # never given fabricated one as prose instead: measured on the live cluster
      # 2026-07-31 across three never-signed-in accounts, one of which leaked exactly
      # that shape (`:::tool\n{"name": "web_search", ...}`), with `unfinished:false,
      # error:false` so nothing downstream knew the turn had failed; reproduced again,
      # differently, under fix attempt 2 above (a truncated `:::search` fabrication and
      # a bare "I'll check the current stable version of Python for you." with no
      # answer — the same failure shapes the original bug report showed, because the
      # tool was, in every one of these attempts, still not actually attached).
      #
      # `mcpServers`, `fileSearch` and `artifacts` on a modelSpec are top-level for the
      # identical reason — `dk()` reads all four (`t.mcpServers`, `t.webSearch`,
      # `t.fileSearch`, `t.artifacts`) off the same object. `executeCode` is excepted:
      # it stays where it was (`preset.executeCode`) because moving it is a distinct,
      # pre-existing defect this item does not touch — see dogfood-findings.md.
      webSearch: true
      preset:
        endpoint: "Enterprise AI"
        model: "__CHAT_MODEL_PRIMARY__"
        artifacts: "default"
        # A real zod key (data-provider/src/models.ts's tModelSpecPresetSchema), unlike
        # `interface.artifacts` above — turns on the codeapi sandbox
        # (enterpriseaiframework-082) for turns on this model spec by default, on top of
        # the per-turn `ephemeralAgent.execute_code` a caller can also set explicitly.
        # Gated together with `endpoints.agents.capabilities`, which defaults to
        # including `execute_code` (data-provider's defaultAgentCapabilities) and is not
        # overridden anywhere in this file.
        #
        # NOT VERIFIED WORKING on the pinned v0.8.7 image — see the `webSearch` comment
        # above this preset block. The same client function that ignores a nested
        # `preset.webSearch` also ignores a nested `preset.executeCode`, by the same
        # mechanism, and this line has sat here unverified since enterpriseaiframework-082.
        # Left exactly as it was: fixing it is out of scope for enterpriseaiframework-222
        # and is flagged in that item's return rather than silently changed here.
        executeCode: true
        # Matches the provider: 200k context, and the full output budget. These models
        # spend part of the output budget reasoning, so a short cap truncates the answer
        # after the thinking rather than producing a smaller answer.
        maxContextTokens: 200000
        maxOutputTokens: 32768
        # TOOL SELECTION (enterpriseaiframework-e6f). This is the "reaches for the right
        # tool on its own" instruction — deliberately generic, naming no specific skill,
        # because skills are TENANT content (guard #10) and this file is platform
        # config. Measured against a live model, before/after this paragraph existed:
        # see tests-live/test_tool_selection.py for the scripted prompts and the
        # before/after tool-selection rates this text is responsible for.
        promptPrefix: |
          Decide for yourself, on every turn, whether a tool would make your answer
          better founded than answering from memory alone. Do not wait to be told which
          one to use, and do not name or ask permission to use one — just use it.

          - Search the web when the question turns on something that could have
            changed since your training, or a specific current fact you are not
            certain of from memory: a version number, a price, a date, a status, a
            score, a fact tied to a specific external page.
          - Run code when the question needs an exact computation, a nontrivial
            transformation of data, or a result that should be verified rather than
            estimated — execute it and report the real output, not a predicted one.
          - Use a loaded skill when one of the skills available to you states a
            purpose matching what the user is asking; its procedure is what this
            deployment has decided is correct for that request, so follow it instead
            of improvising an equivalent yourself.

          Most turns call for none of these — use a tool only when the request
          actually needs it.

          When you produce a self-contained HTML/CSS/JS page, a React component, a
          Mermaid diagram or an SVG, emit it as an artifact.

          The opening fence is EXACTLY three colons, then the word artifact:
          :::artifact{identifier="kebab-case-id" type="text/html" title="A Short Title"}
          ```html
          <!DOCTYPE html> ...
          ```
          :::

          Three colons to open and three to close. Two colons will not render and the
          user will see raw code instead of a running program. Put the entire program in
          one artifact so it runs on its own with no other files.
    - name: glm-4-7
      label: "GLM 4.7 (cheapest)"
      description: "Open weights. $0.40/$1.75 per Mtok — the cheapest priced model in the catalogue."
      # webSearch (enterpriseaiframework-222) — top-level, sibling of `preset:`, same
      # placement and same reasoning as the glm-5-2 spec above (see its comment for the
      # two wrong shapes measured against the live cluster before this one).
      webSearch: true
      preset:
        endpoint: "Enterprise AI"
        model: "__CHAT_MODEL_SECONDARY__"
        artifacts: "default"
        maxContextTokens: 128000
        maxOutputTokens: 32768
        # TOOL SELECTION (enterpriseaiframework-e6f) — same paragraph as the glm-5-2
        # spec above; kept identical rather than DRY'd out because librechat.yaml has
        # no include mechanism and a divergence between the two would be silent.
        promptPrefix: |
          Decide for yourself, on every turn, whether a tool would make your answer
          better founded than answering from memory alone. Do not wait to be told which
          one to use, and do not name or ask permission to use one — just use it.

          - Search the web when the question turns on something that could have
            changed since your training, or a specific current fact you are not
            certain of from memory: a version number, a price, a date, a status, a
            score, a fact tied to a specific external page.
          - Run code when the question needs an exact computation, a nontrivial
            transformation of data, or a result that should be verified rather than
            estimated — execute it and report the real output, not a predicted one.
          - Use a loaded skill when one of the skills available to you states a
            purpose matching what the user is asking; its procedure is what this
            deployment has decided is correct for that request, so follow it instead
            of improvising an equivalent yourself.

          Most turns call for none of these — use a tool only when the request
          actually needs it.

          When you produce a self-contained HTML/CSS/JS page, a React component, a
          Mermaid diagram or an SVG, emit it as an artifact.

          The opening fence is EXACTLY three colons, then the word artifact:
          :::artifact{identifier="kebab-case-id" type="text/html" title="A Short Title"}
          ```html
          <!DOCTYPE html> ...
          ```
          :::

          Three colons to open and three to close. Two colons will not render and the
          user will see raw code instead of a running program. Put the entire program in
          one artifact so it runs on its own with no other files.

registration:
  # Only identities from the realm. Belt and braces alongside ALLOW_REGISTRATION=false.
  socialLogins: ["openid"]

# MCP SSRF allowlist. NEW REQUIREMENT — v0.8.6+ refuses to connect to any MCP server
# whose host resolves into private IP space unless it is exempted here
# (packages/api/src/auth/domain.ts#isMCPDomainAllowed -> isSSRFTarget /
# resolveHostnameSSRF). `mcp-echo` is a container/Service name on a private network, so
# without this the v0.8.7 surface starts perfectly healthy and logs
#   [MCP][echo] Failed to initialize: Domain "http://mcp-echo:8080" is not allowed
#   [MCP] Initialized with 1 configured server and 0 tools.
# — one configured server, zero tools. Nothing user-visible fails until a model tries to
# call the tool and cannot see it. Observed on the v0.8.7 bundle before this block existed.
#
# allowedAddresses (host:port exemptions), NOT allowedDomains. An `allowedDomains` list
# turns SSRF protection off wholesale for MCP —
# MCPServerInspector.inspect sets `useSSRFProtection = !allowedDomains?.length` — whereas
# allowedAddresses exempts exactly one host and one port and leaves the protection on for
# everything else. Same reasoning as NODE_EXTRA_CA_CERTS being scoped to our own CA
# rather than disabling certificate verification.
#
# WARNING — THIS KEY IS FATAL ON v0.8.0. `mcpSettings` does not exist in v0.8.0's
# configSchema, and the schema is parsed with `.strict()` at the top level, so v0.8.0
# rejects the whole file and its loadCustomConfig returns null: no custom endpoint, no
# modelSpecs, no memory, no MCP, and a surface that still answers /health with 200. The
# image pin and this file must therefore move together on every deployment
# (bundle/docker-compose.yml and deploy/k8s/50-chat.yaml — deploy/bin/deploy.sh renders
# THIS file into the cluster's chat-config ConfigMap).
mcpSettings:
  allowedAddresses:
    - "mcp-echo:8080"

# Web search (enterpriseaiframework-0be). Three legs, and only one of them is free out of
# the box — measured against the pinned v0.8.7 image, not read off the docs:
#
#   search  searxng | serper | tavily     SearXNG self-hosts with no key.        FREE
#   rerank  jina | cohere | none          `none` disables it outright.           FREE
#   scrape  firecrawl | serper | tavily   ALL THREE require a paid API key.      NOT FREE
#
# The item this implements assumed the scraper was optional. It is not. LibreChat's own
# auth table (data-schemas webSearchAuth.scrapers) marks firecrawlApiKey, serperApiKey and
# tavilyApiKey all as required, and there is no `none` for scrapers the way there is for
# rerankers. What makes it dangerous rather than merely limiting is the failure mode:
#
#     // @librechat/agents .../tools/search/firecrawl.cjs
#     if (!this.apiKey) return [url, { success:false, error:'FIRECRAWL_API_KEY is not set' }];
#
# That returns before any HTTP request. handleTools.js ignores loadWebSearchAuth's
# `authenticated` flag and builds the tool anyway, so with SearXNG configured and no
# scraper key web_search SUCCEEDS: the model receives titles, links and search-engine
# snippets, and writes a confident answer citing pages that nothing ever retrieved. One
# logger.warn is the only trace. That is the fabricated-citation shape this item exists to
# rule out, so the scraper leg is supplied by webfetch/ (ours, Apache-2.0) speaking
# Firecrawl's /v2/scrape wire contract.
#
# EVERY VALUE BELOW IS A ${...} PLACEHOLDER AND MUST STAY ONE. LibreChat does not read
# these as literals: extractWebSearchEnvVars pulls an environment-variable NAME out of each
# value via extractVariableName, then resolves it. A literal URL written here produces no
# variable name, so the field counts as absent, `requiredAuthFields.length !==
# requiredKeys.length` skips the whole provider, and searchProvider falls back to its
# default of `serper` with no key. Hardcoding `searxngInstanceUrl: http://searxng:8080`
# here — the obvious thing to do — silently disables search. The values live in
# bundle/docker-compose.yml and deploy/k8s/50-chat.yaml.
webSearch:
  searchProvider: searxng
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"

  # `firecrawl` names the PROTOCOL, not the vendor. firecrawlApiUrl points at our own
  # service; a deployment that wants real Firecrawl changes these two values and nothing
  # else. Self-hosted Firecrawl stays a documented swap rather than the default: it is
  # AGPL-3.0 with a closed-source cloud-only anti-bot engine, and it brings Postgres,
  # Redis, a browser pool and its own LLM keys (docs/design/design.md §7).
  scraperProvider: firecrawl
  firecrawlApiUrl: "${FIRECRAWL_API_URL}"
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"

  # ***  READ THIS BEFORE ASSUMING web_search IS UNGROUNDED. RESOLVED: rd 0be, ruling on
  # enterpriseaiframework-405.  ***
  #
  # `rerankerType: none` is free and authenticates, but it has a consequence that is not
  # documented anywhere upstream: WITH rerankerType: none, THE FETCHED PAGE CONTENT NEVER
  # REACHES THE MODEL. The pages ARE fetched — webfetch's /fetchlog shows real 200s and
  # real byte counts for every source — and then the content is dropped on the floor
  # before the prompt is built. The chain, from @librechat/agents' own source:
  #   createReranker({rerankerType:'none'})            -> returns undefined
  #   getHighlights({reranker: undefined})             -> warns, returns undefined
  #   addHighlights()                                  -> source.highlights = undefined
  #   formatSource()                                   -> emits `Summary: <snippet>` and
  #                                                       the Highlights section ONLY if
  #                                                       source.highlights is non-empty
  # format.ts says it outright: "per-source `content` stays in the WEB_SEARCH artifact for
  # citations" — the artifact, not the prompt.
  #
  # MEASURED on this bundle, same question, same model, one config line different:
  #   rerankerType: none   tool output  3,688 chars,  0 highlight blocks  (snippets only)
  #   reranker present     tool output 17,510 chars, 20 highlight blocks  (page text)
  #
  # `jina` with no key is a TRAP, not a fix: loadWebSearchAuth cannot authenticate jina
  # without jinaApiKey, so it leaves rerankerType unset, and createSearchTool then falls
  # back to its OWN default of `cohere` — you write jina and get CohereReranker, which
  # finds no COHERE_API_KEY, logs "Using default ranking" and returns the first N chunks
  # with score 0. `infinity` looks like a free built-in but is UNREACHABLE from this file:
  # the data-provider RerankerTypes enum is jina|cohere|none and webSearchSchema uses
  # z.nativeEnum, so `infinity` never resolves. A real Cohere or Jina key is disqualified
  # by the component-defaults rule (no paid tier in any default path).
  #
  # THE RESOLUTION: `jina` IS the value here, pointed at `rerank/` — ours, Apache-2.0,
  # standard library only, no paid dependency, no provider in the data path — which
  # speaks Jina's own /v1/rerank wire contract with a key WE mint (RERANK_TOKEN). Same
  # trick webfetch/ already plays for the scraper leg: LibreChat's unmodified
  # JinaReranker client is pointed at a service of ours that answers its own protocol.
  #
  # RANKING HONESTY, IN CAPITALS BECAUSE IT WILL OTHERWISE BE MISREAD AS SEMANTIC SEARCH:
  # rerank/app.py ranks with OKAPI BM25 — LEXICAL TERM OVERLAP, NOT SEMANTIC. A chunk that
  # answers the question in different words than the query scores LOWER than a chunk that
  # parrots the query's own words back, even if the parroting chunk says less. The score
  # is a squashed BM25 sum, not a calibrated probability. What this buys is GROUNDING
  # (fetched page text reaches the model); ranking QUALITY is a separate, weaker claim —
  # see rerank/app.py's docstring and tests/test_web_search.py for both held apart.
  rerankerType: jina
  jinaApiUrl: "${JINA_API_URL}"
  jinaApiKey: "${JINA_API_KEY}"

  # Above webfetch's own 20s ceiling would let LibreChat give up on a fetch that is still
  # in flight, which loses content that was retrieved successfully. Below it wastes the
  # difference. 20000 matches WEBFETCH_TIMEOUT_SECONDS.
  scraperTimeout: 20000
  safeSearch: 1

mcpServers:
  # The trivial MCP server (enterpriseaiframework-12b). Proves the wiring ONCE — one
  # tool, `echo`, that returns a value the model cannot produce on its own
  # (`ECHO:<input>`) — before web-search, skills and code-execution are built as real
  # MCP servers on the same path. Deployed as its own Service in the cluster
  # (deploy/k8s/06-mcp-echo.yaml, source in mcp-echo/), reached over streamable-http so
  # LibreChat calls it as a network peer rather than spawning it as a subprocess inside
  # its own container.
  echo:
    type: streamable-http
    url: "http://mcp-echo:8080/mcp"
    chatMenu: true

endpoints:
  custom:
    - name: "Enterprise AI"
      # These two are the operator-agnostic DEFAULTS, overridden per instance at deploy time —
      # they are NOT hardcoded to any operator's backend (that would make this bundle
      # un-hoistable; see docs/design/hoistable-and-operated.md). deploy/bin/deploy.sh
      # substitutes both from the instance's gitignored bundle/.env before rendering the
      # chat-config ConfigMap: CHAT_INFERENCE_BASE (default below) and CHAT_ENDPOINT_APIKEY
      # (default below).
      #
      #   * Default profile (nothing set): a shared virtual key against the bundled LiteLLM
      #     gateway. LibreChat identifies the person by forwarding them in the request `user`
      #     field, which LiteLLM honors as `end_user` — per-user chat billing without a
      #     per-user key. This is the simplest working profile and what a forker gets.
      #   * Per-user-key profile (CHAT_ENDPOINT_APIKEY=user_provided, CHAT_INFERENCE_BASE at a
      #     key-only gateway like freerouter): LibreChat sends each signed-in user's OWN
      #     `<user>::chat` key, chosen from its authenticated session — the key is the
      #     principal, unspoofable. The control plane seeds each user's key into LibreChat's
      #     store so nobody pastes one (control-plane app/chat_keyseed.py, reconciled by
      #     POST /admin/chat/reconcile-keys); the backfill must run before switching this
      #     endpoint to user_provided, or an un-seeded user hits the paste-a-key prompt.
      apiKey: "${CHAT_VIRTUAL_KEY}"
      baseURL: "http://gateway:4000/v1"
      models:
        # Fetched from the gateway so the catalogue has exactly one source.
        default: ["__CHAT_MODEL_PRIMARY__", "__CHAT_MODEL_SECONDARY__"]
        fetch: true
      titleConvo: true
      # The model that NAMES a conversation. It was "fake-small", which is the local
      # bundle's stub provider — harmless on a laptop, and the reason every conversation
      # in the cluster was titled "[fake-provider fake-gpt-small] ack <hex>". The chats
      # themselves were real; only their names came from a stub, which made a working
      # history look like a broken one.
      #
      # Titling is one short call per conversation, so it goes to the cheaper of the two
      # real models rather than the default one.
      titleModel: "__CHAT_MODEL_SECONDARY__"
      modelDisplayLabel: "Enterprise AI"
      # ATTRIBUTION — depends on the profile (see the apiKey/baseURL note above):
      #   * Default (shared key): chat rows carry a per-user `end_user` value — LibreChat's
      #     Mongo ObjectId, forwarded in the `user` field — which the gateway records and
      #     chat_identity.py translates to a username. The rest of this note describes that
      #     path, which is the one in force under the default profile.
      #   * Per-user-key profile: each request carries the user's own `<user>::chat` key, so
      #     the spend row's alias already names the person, unspoofably — the key is the
      #     principal, exactly like every other surface. The forwarded `user` still arrives but
      #     attribution no longer depends on it, making the id->name translation below
      #     vestigial for those rows (principal_label passes a username alias through
      #     untouched). Retiring it under that profile is tracked (enterpriseaiframework-e59).
      #
      # ---- the shared-key path (default profile): forwarded end_user ----
      # chat rows carry a per-user `end_user` value (LibreChat's Mongo ObjectId), so chat
      # spend is attributed per person by translating that id.
      #
      # What arrives there is LibreChat's own primary key (a 24-char Mongo ObjectId),
      # which is correct, stable, and useless to show a human.
      # control-plane/app/chat_identity.py translates it, read-only, over one collection.
      #
      # That translation is now applied inside the query itself
      # (metering.spend_by_user_and_surface -> chat_identity.attribute), so `make spend`,
      # the portal and the operator console name the same people by construction. It used
      # to be applied only in the portal, which is why the CLI bill printed hex while the
      # console printed names — same data, same query, two answers (rd f8c, finding 34).
      #
      # NOTHING HERE FORWARDS THE USER, AND NOTHING NEEDS TO. LibreChat sets the outgoing
      # `user` field to its own Mongo _id unaided — measured in the bundle 2026-07-29 by
      # sending one real message as a signed-in user and reading the gateway's raw
      # end_user column, which held exactly the _id that /api/user reports for that
      # account. Asserted by TestChatPrincipalOnTheOneBill so a LibreChat upgrade that
      # changes it fails loudly instead of quietly refilling the bill with hex.
      #
      # The original reasoning still stands as a reason not to add one:
      # {{LIBRECHAT_USER_ID}} substitution is only demonstrably supported for headers, and
      # an unsubstituted placeholder would write the literal string into the bill as a
      # username — worse than having no per-user chat attribution at all.
